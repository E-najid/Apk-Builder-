# {{APP_NAME}}

An Android app built in Java, created with APK Builder.

Open the `app/src/main/java/` directory to start editing. The build runs
on GitHub Actions every time you press Build in APK Builder.
