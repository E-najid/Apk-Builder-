# {{APP_NAME}}

Flutter app — built by [APK Builder](https://github.com/E-najid/Apk-Builder-).

- App code lives in `lib/main.dart`.
- Android project lives in `android/`.
- Every push to `main` builds a debug APK; tag a release (`v*`) to publish.

Build locally: `flutter pub get` then `flutter build apk`.
