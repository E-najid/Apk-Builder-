# {{APP_NAME}}

React Native app — built by [APK Builder](https://github.com/E-najid/Apk-Builder-).

- App code lives in `App.tsx` (JS/TS).
- Android project lives in `android/`.
- Every push to `main` builds a debug APK; tag a release (`v*`) to publish.

Build locally: `npm install` then `npx react-native run-android`.
